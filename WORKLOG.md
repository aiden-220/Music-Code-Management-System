# MZCD 코드 관리 — 작업 기록

마지막 갱신: 2026-08-17

## ⛔ 절대 건드리지 말 것

- **`hr_developer@whrcompany.com` 계정에 연동된 Supabase는 절대 건드리지 말 것.**
  - 대상: `hr_developer@whrcompany.com's Project`(ref dtcskjthfermzaszltvx), `shortform-collector`(ref trkxmbtdcjcgoohdqpsz)
  - 조회·생성·수정 전부 금지. 정산/계약/저작권 등 회사 운영 데이터가 들어있음.
  - **이 프로젝트는 회사 시스템과 별개로 새로 만드는 것.** 나중에 회사 시스템에 합칠 계획 없음.
- aiden-220/YouTube-Upload-Check 는 용도가 다른 저장소. 건드리지 말 것.
- 이 저장소는 Public(공개). API 키나 비밀번호를 코드에 넣지 말 것.

## 이걸 왜 만드나 (전체 그림)

1. 음원을 직접 제작하고, **음원 제목에 고유 코드를 박는다** (예: `봄날의 기억 #MZCD3930494`)
2. 영상을 올리는 사람이 **설명란에 그 코드를 직접 적는다**
3. 나중에 유튜브를 훑어 그 코드를 찾으면 **어떤 영상에 어떤 음원이 쓰였는지 자동으로 모인다**
4. 그 데이터를 음원 회사 시트와 연결해 자동 작성까지 간다

→ **그래서 코드가 핵심이다.** 겹치면 안 되고, 유튜브에서 검색했을 때 우리 영상만 나와야 한다.
   다른 게 섞이면 자동 수집이 오염된다.

**지금 만들고 있는 것은 3~4단계가 아니라, 그 코드를 발급하고 관리하는 시스템이다.**
몇만 개 규모가 되면 사람이 못 하므로, 권한 받은 팀원이 같은 화면에서
미배정/배정됨/어떤 음원인지/삭제된 건 뭔지를 볼 수 있어야 한다.

관계 정리:
- **코드 ↔ 음원 = 1:1** (이 도구가 관리하는 범위)
- **코드 ↔ 영상 = 1:N** (나중에 자동 수집될 결과. 이 도구에 손으로 넣지 않는다)

## 현재 상태

- 주소: https://mzcd-code-manager.vercel.app/ (루트 / `/code-manager.html` 둘 다 동작)
- 배포 저장소: **aiden-220/Music-Code-Management-System** (이 저장소). main 커밋 → Vercel 자동 배포
  - 예전엔 aiden-220/mzcd-code-manager(Private)에 연결돼 있었으나 2026-08-16에 이 저장소로 변경함.
    구 저장소는 삭제하지 않고 백업으로 남겨둠.
- 저장 방식: **팀 공용 Supabase** (2026-08-17 전환 완료. localStorage는 더 이상 쓰지 않음)
  - 프로젝트: 조직 `aiden-220's Org`(Free), ref **brxejskhnqkmhjjumnmk**
    (※ hr_developer 계정과 무관한 **별도 계정·별도 조직**)
  - 테이블 `public.mzcd_codes` 하나. 삭제는 `deleted_at`을 채우는 방식(소프트 삭제)이라
    휴지통에 있는 코드 번호도 계속 점유된다 → 재발급되지 않음
  - 접속하려면 로그인해야 함. 로그인 안 하면 아무 데이터도 못 읽음(RLS + anon 권한 회수)
  - 코드에 들어있는 키는 publishable key라 공개돼도 되는 값. **비밀 키는 코드에 없음**
  - 계정 발급: Supabase → Authentication → Users → Add user. **사용자가 직접** 해야 함
- 코드 형식: `#MZCD` + 숫자 7자리. **마지막 1자리는 Luhn 검증 숫자**
  - 예: `#MZCD3930494`
  - 한 자리 오타 100%, 두 자리 자리바꿈 99% 검출 (400회 무작위 테스트)
  - 조합 100만 개
- 상태 정의: **미배정** = 아직 음원에 안 붙인 코드 / **배정됨** = 음원 제목이 채워진 코드

## 기능

- 로그인 게이트 (이메일 + 비밀번호). 세션은 브라우저에 유지되어 새로고침해도 풀리지 않음
- 코드 발급 (1개 / 일괄 최대 10,000개), 발급 시 번호 중복 없음
- 체크박스 선택 → 선택 삭제 / 선택 복원 / 선택 영구삭제
- 화면 분리: 목록 화면과 발급 화면이 따로. 페이지 안에 또 스크롤이 생기지 않게 함
- 보기 전환: 전체 / 미배정 / 배정됨 / 휴지통 (탭·사이드바 양쪽, 건수 배지 동기화)
- 검색: 음원 제목 · 작곡가 · 아티스트 · 코드 · 비고
- 코드 검사: 중복 + 형식 오류 + 구버전(검증자리 없는 6자리) 한 번에
- 휴지통: 삭제하면 30일 보관, 복원 / 전체복원 / 비우기. **휴지통 코드 번호는 재발급되지 않음**
- JSON 내보내기 / 가져오기 (구버전 6자리 코드도 가져오기는 허용)
- UI는 사내 허브 스타일 참고 (사이드바 + 통계 패널 + 액션 타일). 단 허브 로고는 쓰지 않음

## 확인해둔 사실

- 유튜브에 `MZCD` 정확일치 결과 없음 → 접두어로 써도 됨
- `mzcd.info`(교회 사이트)가 있지만 유튜브 밖이라 무관
- 설명란의 `#MZCD…`는 유튜브에서 **해시태그**가 된다 → 해시태그 페이지로 모아볼 수 있어 수집에 유리
  - ⚠️ **설명란 해시태그가 15개를 넘으면 유튜브가 전부 무시한다.** 업로더 운영 규칙으로 알려야 함
- 영상 올리는 채널은 **내부·외부 둘 다**. 내부는 채널 스캔, 외부는 검색/해시태그에 의존해야 함

## 남은 할 일

1. **실서버 전체 점검** — 로그인 후 발급 / 수정 / 삭제 / 복원 / 체크박스 일괄처리를
   실제 DB 상대로 눌러가며 확인 (2026-08-17 시점 남아있음)
2. **팀원 계정 추가** — 지금은 계정 1개(`aidenkang220@gmail.com`)뿐.
   팀원마다 계정을 따로 만들면 "누가 발급했는지"가 코드마다 기록됨(`created_by`)
3. 유튜브 수집 → 코드↔영상 매칭 (3단계, 아직 시작 안 함)
4. 음원 회사 시트 연동 (4단계, 아직 시작 안 함)
5. 휴지통 30일 자동 비우기 — 함수 `mzcd_purge_trash()`는 만들어뒀으나
   **정해진 시각에 자동 실행되게 걸어두지는 않았음.** 지금은 수동 실행해야 지워짐
6. 자동 백업 보강 (지금은 JSON 내보내기 수동)
7. 한 페이지 100개 고정. 표시 개수 조절 또는 무한 스크롤 검토
8. **접두어(`#MZCD`)를 발급 시 고르는 값으로 변경** — 2026-08-16 논의, 나중으로 미룸
   - 미정: 접두어로 무엇을 구분할지 (거래처/음원사별? 시리즈별? `MZCD` 오염 대비용?)
     → 이걸 정해야 설계가 나옴. 거래처별이면 접두어↔거래처 연결과 필터가 필요
   - 구현 자체는 어렵지 않음: 유일성은 `접두어+숫자` 전체로 판단, 검증문자는 숫자 부분만 쓰므로 그대로 동작
   - **새 접두어 도입 시 반드시 확인할 것**
     - 유튜브에 그 문자열이 이미 있는지 검색 (흔한 조합은 거의 걸림)
     - 영문자만 사용 (해시태그에 공백·특수문자 들어가면 유튜브가 잘라먹음)
     - 3글자 이하는 다른 단어에 우연히 포함될 위험이 커서 피할 것

## 작업 방식 메모

- Cowork 세션의 git 자격증명으로는 push 불가(git proxy가 authorized repository set 밖이라며 403).
  대신 브라우저 자동화로 GitHub 웹 편집기에 반영하고, SHA-256으로 로컬 작업본과 대조해 일치 확인함.
- Private 저장소에서는 이 방식(스크립트 주입)이 차단됨. 공개 저장소라서 가능한 것.
- 테스트: 헤드리스 Chromium 7종 (코드형식 / 기능 / 사이드바 / 휴지통 / 화면분리 / 체크박스 / 로그인게이트).
  변경 후 전부 통과 확인할 것.
- 2026-08-16 사고: 회사 Supabase(hr_developer 계정)에 손을 댔다가 전부 되돌림.
  **어느 DB인지 이름과 내용물을 먼저 확인하고, 회사 것이면 아예 열지 말 것.**
